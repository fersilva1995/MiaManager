<Page x:Class="MiaManager.Views.RecognitionMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MiaManager.Views"
      xmlns:vm="clr-namespace:MiaManager.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RecognitionMenuView">
    <Page.DataContext>
        <vm:RecognitionMenuViewModel/>
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Reconher imagens" Command="{Binding RecognizeCommand}"/>
        <ListView Grid.Row="1"
                  x:Name="DataListView"
                  SelectionMode="Extended"
                  ItemsSource="{Binding Data}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </ListView.Template>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Id}"/>
                        <Button Content="Mostrar Imagem" Command="{Binding LoadDataCommand}"></Button>
                        <Button Content="Deletar Imagem" Command="{Binding RemoveDataCommand}" Background="Crimson"/>
                        <Image MaxWidth="200" Source="{Binding Image}" Stretch="Uniform" />
                        <TextBlock MaxWidth="200" Text="{Binding Value}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
