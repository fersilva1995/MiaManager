<Page x:Class="MiaManager.Views.SvmView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MiaManager.Views"
      xmlns:vm="clr-namespace:MiaManager.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SvmView">
    <Page.DataContext>
        <vm:SvmViewModel/>
    </Page.DataContext>

    <Grid x:Name="Main">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="Header" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Id}"
                           FontSize="20"/>
                <TextBox Text="{Binding Name}"
                         FontSize="20"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <CheckBox IsChecked="{Binding CreateNegative, Mode=TwoWay}"
                          Content="Criar classe negativa"
                          FontSize="20"/>
                <CheckBox IsChecked="{Binding CreateUnknown, Mode=TwoWay}" 
                          Content="Criar classe desconhecido"
                          FontSize="20"/>
            </StackPanel>
            <Button Grid.Column="2"
                    Content="Salvar"
                    Command="{Binding UpdateSvmCommand}"/>
        </Grid>
        <Grid x:Name="Body" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0"  
                      AutoGenerateColumns="False"  
                      CanUserAddRows="False"
                      HeadersVisibility="All"
                      ItemsSource="{Binding Users}"
                      SelectionUnit="{Binding ElementName=selectionUnitComboBox, Path=SelectedValue}">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}">
                        <DataGridCheckBoxColumn.Header>
                            <Border Background="Transparent">
                                <CheckBox IsChecked="{Binding DataContext.IsAllSelected, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            </Border>
                        </DataGridCheckBoxColumn.Header>
                        <DataGridCheckBoxColumn.HeaderStyle>
                            <Style TargetType="{x:Type DataGridColumnHeader}"
                                   BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Center" />
                            </Style>
                        </DataGridCheckBoxColumn.HeaderStyle>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Id">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Id"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Name">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Name"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
