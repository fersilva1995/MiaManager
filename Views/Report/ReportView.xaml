<Page x:Class="MiaManager.Views.ReportView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MiaManager.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450"
      d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:vm="clr-namespace:MiaManager.ViewModels"
      Title="ReportView">
    <Page.DataContext>
        <vm:ReportViewModel/>
    </Page.DataContext>
    <Grid x:Name="Main">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="Header" 
              Grid.Row="0"
              Margin="20 10 20 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Id}"
                           FontSize="20"/>
                <TextBox Text="{Binding Name}"
                         FontSize="18"/>
            </StackPanel>
            <StackPanel Grid.Column="1"
                        Margin="0 0 20 0">
                <TextBlock Text="Modelo"
                           FontSize="20"/>
                <ComboBox ItemsSource="{Binding Svms}" 
                          SelectedItem="{Binding SelectedSvm, Mode=TwoWay}" 
                          DisplayMemberPath="Name"
                          FontSize="18"/>

            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Threshold"
                           FontSize="20"/>
                <materialDesign:NumericUpDown FontSize="18"
                                              Maximum="100"
                                              Minimum="0"
                                              Value="{Binding Threshold}"/>
            </StackPanel>

            <Button Grid.Column="3"
                    Content="Salvar"
                    Margin="10 0 10 0"
                    Background="ForestGreen"
                    BorderBrush="ForestGreen"
                    Command="{Binding UpdateReportCommand}"/>
            <Button Grid.Column="4"
                    Margin="10 0 10 0"
                    Content="Executar"
                    Background="DarkOrange"
                    BorderBrush="DarkOrange"
                    Command="{Binding ExecuteReportCommand}"/>
        </Grid>
        <Grid x:Name="Body" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0"
                      x:Name="DataGridExample" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      ItemsSource="{Binding Targets}">
                <DataGrid.Columns>

                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Feature Count" Binding="{Binding CurrentFeatureNumber}" Width="*" />
                    <DataGridTextColumn Header="Image Count" Binding="{Binding FileCount}" Width="*" />
                    <DataGridTemplateColumn Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Arquivos"
                                        Command="{Binding LoadInputFilesCommand}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Text="Resultados"
                       Grid.Row="1"
                       VerticalAlignment="Center"
                       FontSize="20"/>
            <DataGrid Grid.Row="2"
                      x:Name="DataGridResults" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      ItemsSource="{Binding Reports}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" />
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Threshold" Binding="{Binding Threshold}" Width="*" />
                    <DataGridTextColumn Header="Precisão (%)" Binding="{Binding Precision}" Width="*" />
                    <DataGridTextColumn Header="Recall (%)" Binding="{Binding Recall}" Width="*" />
                    <DataGridTextColumn Header="F1 (%)" Binding="{Binding F1}" Width="*" />

         
                    <DataGridTemplateColumn  Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remover" 
                                        Background="Crimson"
                                        BorderBrush="Crimson"
                                        Command="{Binding RemoveReportResult}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Mostrar" Command="{Binding ShowReportResult}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>

</Page>
